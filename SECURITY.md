# Безопасность проекта Churn Prediction MVP

**Инструкция для команды**

**Где хранятся пароли**

| Секрет | Где хранить | Где НЕ хранить |
|--------|-------------|---------------|
| PostgreSQL пароль | `.env` (локально) | Код, Git |
| Elasticsearch пароль | `.env` (локально) | Код, Git |

**Чеклист перед коммитом**

```bash
# 1. Проверить что попадёт в коммит
git status

# 2. Убедиться что .env НЕ в списке
# Если видите .env — НЕ добавляйте!

# 3. Проверить .gitignore
cat .gitignore

# 4. Только потом коммитить
git add .
git commit -m "message"